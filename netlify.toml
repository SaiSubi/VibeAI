[build]
  command = "echo 'V2 frontend files ready - no build needed'"
  publish = "frontend"

[build.environment]
  NODE_ENV = "production"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
