[build]
  command = "echo 'V2 static files ready - no build needed' && exit 0"
  publish = "vibeai-ui/dist"

[build.environment]
  NODE_VERSION = "18"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
